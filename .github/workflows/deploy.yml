name: Pygame Web Build

on:
  push:
    branches: [ "main", "master", "web-version", "irem_game_coding" ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-n-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Build with pygbag
      run: |
        python -m pip install pygbag
        # We build the current directory (which contains main.py)
        python -m pygbag --disable-sound-format-error --build .
    - name: Deploy to GitHub Pages
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        folder: build/web
        branch: gh-pages
